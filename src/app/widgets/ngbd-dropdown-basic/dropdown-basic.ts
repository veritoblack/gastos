import {Component} from '@angular/core';

@Component({
  selector: 'ngbd-dropdown-basic',
  templateUrl: './dropdown-basic.html'
})
export class NgbdDropdownBasic {

// TODO Split this in one dropdown, and add the values and everything into the dates component
// TODO Add the event --> On select or stuff for filtering the values
  
}
